console.time;
$i = 2;
$n = 0;
$max = first;
{n < max}.while{
    $k = 2;
    $ispr = true;
    {k*k <= i}.while{
        {i%k == 0}.if{ispr = false};
        k++
    };
    {ispr}.if{n++};
    i++
};
console.timeEnd;
i-1
